import HomePage from "./pages/home-page/page"; 
import { Theme, ThemeProvider } from "@mui/material/styles";
import theme from "./theme";

// let themeStyle:Theme = theme();

export default function Home() {
  return (
    <HomePage />  
    );
}